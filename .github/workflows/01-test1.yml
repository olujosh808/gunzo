name: First run-name
on:
  push:
    branches:
      - main
jobs:
  simple-job:
    runs-on: ubuntu-latest
    steps:
      - name: step 1
        run: |
          pwd
          ls -la
          hostname
      - name: checkout
        uses: actions/checkout@v5
      - name: step 3
        run: |
          pwd
          ls -la
          hostname
